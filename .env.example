VITE_SUPABASE_URL=https://YOUR_PROJECT.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=YOUR_PUBLISHABLE_KEY

# Only used by the local UI preview when Supabase is not configured.
VITE_BEAN_GROWTH_MODE=development

# Configure these in the Supabase dashboard, never in VITE_ variables:
# Twilio Account SID
# Twilio Message Service SID
# Twilio Auth Token
